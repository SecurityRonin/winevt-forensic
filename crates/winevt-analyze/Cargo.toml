[package]
name = "winevt-analyze"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Structured field analysis of Windows Event Log records: timeline, session reconstruction, PowerShell block reassembly, and frequency analysis."

[lib]
name = "winevt_analyze"

[dependencies]
evtx = "0.11"
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
