[package]
name = "winevt-binxml"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
thiserror = { workspace = true }
chrono = { workspace = true }
serde_json = { workspace = true }
winevt-core = { workspace = true }

[dev-dependencies]
# Differential oracle for the real-fixture parity test only — NOT a runtime dep.
evtx = "0.11"

[lints]
workspace = true
